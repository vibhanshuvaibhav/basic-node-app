
const {
    dog,
    cow,
} = require('./animal');

class Deal{
    constructor(say, animal= 'dog'){
        let text = `
         ________________________________________
        ${say}
         ----------------------------------------
        `;
        console.log(`${text}${eval(animal)}`);
    }
}

module.exports = Deal
